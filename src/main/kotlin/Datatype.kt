fun main(){
    // data type
    //INTEGER (Byte, Short, int, Long)
    //FLOATING POINT (float, Double)
    //Boolean (True, False)
    //CHARACTER (char, string)
    var score = 5 //type inference
    //score = 3.14 //once the variable type is defined we can not change it now , Type checking at compile time

    //Explicitly defining the datatype of a variable
    var secondScore: Int  = 12
    println(secondScore)

    var temp: Double = 3.14
    var isRaining: Boolean = true
    var alphabet: Char = 'a'
    var message: String = "My name is manoj kumar paswan"

    println(temp)
    println(isRaining)
    println(alphabet)
    print(message)

}