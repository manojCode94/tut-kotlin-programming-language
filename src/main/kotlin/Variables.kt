//kotlin is a statically typed language
//Means compile type checking will be happened
//we need to defined the data types
fun main() {
    //var - this way of creating variable we can reassign it
    var score  = 5
    score = 10
    score = 50
    println(score)

    val message = "Hello world"
    println(message)

    //val -- type variables can not be reassigned
    //message = "Changed world"
    //compile time checking
}